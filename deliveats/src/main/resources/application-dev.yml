spring:
  config:
    activate:
      on-profile: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/deliveats
    username: root
    password: 13579

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: VqsjDGgZ3cB2xex2N79NRImBPf5gAQzz6wnFsnqllVY=
  access-token-validity-seconds: 1800
  refresh-token-validity-seconds: 604800

logging:
  level:
    root: DEBUG
    com.example.deliveats: DEBUG